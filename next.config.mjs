/** @type {import('next').NextConfig} */
const nextConfig = {
  // Здесь могут быть твои будущие настройки
  // reactStrictMode: true, // это стандартная настройка, если она была, можешь раскомментировать
};

export default nextConfig;